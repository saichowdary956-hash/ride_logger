# ride_logger

This project is tracked with Git. To initialize and push it to your own remote:

1. `git init`
2. `git add .`
3. `git commit -m "Initial commit"`
4. `git branch -M main`
5. `git remote add origin <your-remote-url>`
6. `git push -u origin main`
